# Genesis session handoff

Checkpoint: 2026-09-08, `genesis-m5a-v1`. Read this first after a context reset; inspect `git status` and the current commit/CI before making changes. This note preserves project context, not a claim that the conversation runtime was compacted.

## Latest direction and research checkpoint

The user requested an algorithmic research pause, then explicitly asked to save it in `./research` and continue with an updated roadmap. Research is in `research/2026-09-08-coherent-world-generation.md`; **`docs/Active-Roadmap.md` is now authoritative**. The original roadmap is preserved with a supersession notice. Earlier geology-first next-work preferences below are historical.

R0 is complete. R1a adds Java21-only `oracle/BoundaryFlowSummary` and `BoundarySummaryGates`: lossless single-receiver finite DAG elimination into exact boundary transfers, nested coarsening, stable original edge-source identities, multiple exits and region re-entry. Complete upstream graph is an input, not discovered cheaply. 400 seeded independent source-walk comparisons pass; spatial fixture reduces 384 nodes to 105 with 1,608 exact runoff units. Worst-case alternating-owner graph retains 256/256. Reports: `build/boundary-summary.json`, inspected `build/gallery/boundary-summary.png`. No new dependencies, production fields, Params, version change, viewer changes or historical golden updates.

R1b is now implemented in Java21-only `oracle/ConservativeRunoff` and `ConservativeRunoffGates`. Supplied finite cell forest + unique coordinates + nonnegative reference preferences + one net-runoff budget per terminal; precomputed subtree weights; top-down exact largest-remainder allocation along the queried ancestor chain. Each upstream catchment partitions into the local cell and immediate tributary subtrees. BigInteger protects preference sums/products; results are immutable longs. 500 allocation cases and 200 forests, independent source/preference walks, R1a composition, dry/extreme/invalid/cold/spatial/order/concurrency pass. Spatial fixture: 384 cells, 1,000,003 root units for seeds 42/-1, max 22 splits/73 slots; 1024-chain requires 1024 splits/2047 slots, star root 1024 slots. Inspected image: `build/gallery/conservative-runoff.png`; report: `build/conservative-runoff.json` (CI artifact). See `research/conservative-runoff-experiment.md`.

Important R1b limitations: full finite preprocessing still required, drainage-tree depth can be linear, input topology is fixed rather than a proven world refinement, and largest-remainder allocation is not budget-monotone (tested 25->26 unit counterexample). Choose a monotone alternative or explicitly bounded quantization before physical climate coupling. No new production fields, parameters, version, viewer layers or old goldens.

R2a now adds Java21-only `MaritimeEnvelope` and `MaritimeEnvelopeGates`. One middle-half-jittered site per S cell; exact nearest Voronoi owner from 25 sites; ocean reserve if a +/-m box fails strict owner clearance (`gap <= 2*m*siteL1`). S divisible64 in [64,2^20], m in [1,S/8]. Larger 9x9 BigInteger/corner reference: 1280 cases plus bounds/interior/extreme/cold/concurrent checks. A domain lies in its address-centered 3S box; it is not a watershed identity. See `research/root-terminal-design.md` for derivations and the terrain-first/constructive candidate comparison. Continuous ocean-reserve reasoning is not claimed as completed discrete ocean/terminal classification.

The seed42 current-continent audit at S524288, step8192, 256^2 samples has 17626 land points and 1040 cross-owner land edges. Collars 8192/16384/32768 remove 935/1851/3608 land samples respectively and eliminate cross-owner candidate-land edges. Inspected image `build/gallery/maritime-envelope.png` shows visible cuts through land. **Do not adopt the late-clipped mask**; it is a cost baseline, not a production fix. `build/maritime-envelope.json` is a CI artifact. No core fields/version/Params/viewer or old goldens changed.

Next: R2b compare continents authored with maritime reservations known from the start, multiple seeds and high-coverage/merged layouts, and complete interior-water/terminal policies. R2 remains in progress; bounded terrain-first solving is the proposed first R3 baseline only after geography acceptance, with constructive watershed generation still an alternative. Do not confuse the 25-site lookup with full cold watershed cost. Full R4-R8 geology/climate/surface acceptance precedes Minecraft R9.

R2b screening additionally compares raw scaffold, clipped raw scaffold, and whole-object admission (all disk bounding-square corners safely inside one convex domain). Three seeds x coverages45/100, grid128^2 step16384, same domain spacing/collar. Whole-object policy retains only about half or less of land; high-coverage seed42 largest sampled D8 component is 7702 original, 580 clipped, 294 admitted. Both retrofit policies are rejected, not promoted. These raw counts differ from the prior interpolated-coast audit. Reports/images: `build/maritime-object-admission.json`, inspected `build/gallery/maritime-object-admission.png`. Crop-touching component counts are recorded, not global-continent claims. Next work still needs authored geography or a better separation of continent-scale and watershed-scale commitments; no automatic one-island-per-root fix.

## Connected-land research checkpoint

Latest R2b positive experiment: Java21-only `AuthoredLandmass` + `AuthoredLandmassGates`, documented in `research/authored-landmass-experiment.md`. Complete canonical 193x193 grid (64 cells/S) inside MaritimeEnvelope 3S box; eligible own nonreserve vertices; positive-cost D4 shortest-path growth, exact selected prefix at requested percent. One connected land fixture, not a river tree or production continents. D4 water flood starts only from reserve samples; other-owner nonreserve interiors are unmodeled, enclosed pockets remain code2. Independent union-find checks land/water components. Ten fixtures plus cold/order/coordinate/concurrency/nested-coverage/invalid guards; full-root query near numeric guard rejects instead of clipping. Main S524288 m16384 seeds42/-1/137 at40/65%; source counts4217/3333/3601, land1686/2741,1333/2166,1440/2340. Seed-1 65% retains one unconnected water sample. Image `build/gallery/authored-landmass.png` inspected, JSON full construction counts/timings `build/authored-landmass.json` uploaded by CI. Core version/fields/viewer unchanged. Next: adjoining-domain/scale assessment and actual routing-terminal/depression contracts on these provisional fixtures. Single-landmass/single-scale domain bias remains unaccepted; R2 stays open.

## Latest mosaic checkpoint

`AuthoredMosaicGates` now runs twelve multi-domain tests (seeds42/-1/137, S65536/524288, coverage65/90, collarS/32). Each 257-square display covers [-S,3S] and resolves 28-30 complete canonical roots. Independent sampled D4/D8 counts with synthetic counter fixtures, cross-owner D8 separation, nested coverage and cold crop/order/stride/concurrent reconstruction pass. Reports `build/authored-mosaic.json`, inspected `build/gallery/authored-mosaic.png`; CI uploads both. At high coverage sampled land is76-78%, yet still cell-shaped separated islands. Reject this exact layout as production continents; larger S does not remove the restriction. Retain as finite test input. Full reasoning and next explicit active-domain routing contract in `research/authored-mosaic-experiment.md`; stop morphology tuning on this fixture. Next code: active-mask barriers (not high elevations), integer supplied heights/net runoff, ocean-certified terminals, explicit depression overflow, unresolved disconnected regions, independent minimax/source-walk mass tests. Existing FiniteHydrology has no active mask and assumes unit rain; cannot safely just feed it the fullbox. Core/version/viewers untouched; R2/R3 still open.

## Mixed-boundary checkpoint (2026-09-09)

Latest user direction: shared deterministic ridge/ocean boundaries allow land to connect while actual divides isolate surface runoff; target50-55% total land. Implemented Java21-only `MixedBoundaryEnvelope`, `MixedBoundaryLandmass`, `MixedBoundaryGates`, documented in `research/mixed-boundary-experiment.md`. Fixed nearest-owner sites; mutual-nearest matching produces at most one ridge partner/cell. Sorted edge IDs, other faces ocean, ocean wins at mixed junctions. This bounded two-cell grouping is a first fixture, NOT final continent architecture. Full193-square root growth makes ridge bands mandatory and targets53% of ALL owned vertices; impossible constraints reject. Independent water checks retain unconnected pockets. Four mosaics/116 full roots: view51.27-52.75% land, complete-cell52.986-52.989%, 5-7 actual D4 ridge joins per display. 144 larger-support BigInteger nearest cases, corner clearance/midpoint checks, cold crop/order/stride/concurrency. Reports `build/mixed-boundary.json`, inspected `build/gallery/mixed-boundary.png` uploaded by CI. No core/Params/version/viewer/golden change. Full local suite passes. Next: explicit-active routing plus actual shared crest constraints and independent no-leak tests; ridgeStrength is NOT elevation and blocked routing edges alone don't prove a physical divide. Larger groups, curved geometry, climate/tectonic coupling remain open.

## Active drainage checkpoint (2026-09-09)

Latest work: Java21 `ActiveHydrology` is a separate finite D4 minimax solver with active mask, explicit absorbing terminals, long net-runoff sources and undirected integer edge crests. Uses relaxation (first discovery is not final for edge saddles); immutable results, checked arithmetic, no edge fallback. `ActiveHydrologyGates`: 300 independent repeated-relaxation/source-walk cases plus barriers/diagonal isolation, cheaper detour, extremes, invalids, result mutation isolation and concurrent solves. `PairedDrainageGates`: three actual mixed-boundary pairs, full union has finite600 crests but NO cross-owner prohibition; restricted local interiors with shared ocean terminals agree exactly for filled levels/receivers/flux. Totals seeds42/-1/137:22005/22673/21464 discharged,0 unresolved. Synthetic beds[-12,228] and rainfall weights1..9, not geology/climate. Analytic lowered120->30 pass changes spill80->30 and exposes cross-divide flow, unlike a falsely isolated cell. Report `build/paired-drainage.json`, inspected `build/gallery/paired-drainage.png`; CI artifact. Read `research/paired-drainage-experiment.md`. Next actual shared finer crest mesh and diagonal no-leak tests; edge saddles alone aren't continuous terrain. User's geology direction is recorded with USGS sources: collision/subduction/rifting/transform need distinct relief; plate boundaries need not be divides. No production/viewer/golden changes; R2/R3 remain open.

## Plate-vector forcing checkpoint (2026-09-09)

User asked to shape shared boundaries from both plates' directions/speeds. `BoundaryForcing` (Java21 oracle) now reads EXISTING core PlateTopology owner/neighbor/vectors/crust/age, sorted unsigned edge IDs, integer normal/shear projections. Quiet distinct from transform. Profiles: continental collision, subduction trench/arc with explicit descending-side convention, continental rift/shoulders, ocean spreading ridge/notch, mixed margin; shear retained without invented vertical relief. Fixed half-widthS/4, speed scales anomaly, no time simulation. Bounded internal site halo handles +/-2^40 queries without clipping; public guard unchanged. `BoundaryForcingGates`: 300 BigInteger/core comparisons plus side/common-motion/speed/profile/age-tie/invalid/cold/concurrency checks. Controlled forcing couples to active drainage: base pass120 + collision64 ->184 preserves spill80; rift-48 ->72 leaks, isolated-cell assumption fails. Report `build/boundary-forcing.json`, inspected `build/gallery/boundary-forcing.png`. Read `research/boundary-forcing-experiment.md`. IMPORTANT closest-edge switching has seams; multi-edge/junction composition and actual finer terrain remain next, not production adoption. Plate/drainage IDs remain separate. Core/Params/viewer/goldens unchanged; 50-55% land target unchanged.

## Fine crest mesh checkpoint (2026-09-09)

After plate-forcing commit40fcf33 passed Windows/Linux CI, continued with `CrestMesh`: immutable one-level(2w-1)x(2h-1) mesh, original nodes unchanged, active cardinal midpoints=max(end beds,crest), all-four-active centers=max4midpoints. Triangles join the two edge midpoints in each fine quad; integer barycentric evaluator. Added nodes have0rain/no terminals. `ActiveHydrology.solveD8Surface` is a separate vertex-only audit; old D4 API/order unchanged. `CrestMeshGates`:150 independently relaxed coarse/fine cases, source walks, diagonal triangle monotonicity, extremes/cap/input-mutation and lowered120->30 pass test. All3 paired fixtures refine49601->197505 vertices, preserve original spills and discharge totals. Inspected `build/gallery/crest-mesh.png`, report `build/crest-mesh.json`; paired report adds fineD8 agreement. IMPORTANT only spill/reachability/totalmass preservation: receivers/intermediate flux/tied outlet shares can change. Not full cascade or continuous-gradient hydrology. Read `research/crest-mesh-experiment.md`. Next composed plate-profile junctions and explicit port/outlet-preserving constraints on finer terrain. Core/viewer/goldens untouched.

## Soft junction checkpoint (2026-09-09)

Latest user priority: work toward a new tectonic generation viewer, not wait for all hydrology research. Added Java21 `JunctionForcing` and `JunctionForcingGates`, documented in `research/junction-forcing-experiment.md`. Reuses existing plates, with 25-site bounded squared-distance-excess support, compact cubic site weights and canonical unordered pair profiles. Normalizes by max(1,sumPairWeights); positive/negative net pair contributions exposed separately. No nearest-profile winner switch; ideal recipe is C0 but NOT slope-continuous. Includes soft pairs without a certified shared Voronoi face (explicit four-way test includes 6 pairs), not exact face geometry or preserved crest constraints. No production/core/Params/viewer changes.

720 tests across seeds/spacing/jitter/signed/extreme/query-seam coordinates match a 121-site reference exactly and a separate ordered-pair reduction within tolerance. Controlled triple/four-way, speed/quiet/common-position-motion, input-order/cold/crop/concurrency and a versioned 1024-point SHA-256 fingerprint pass. Sampled max one-block height delta6.640900929892382 at tested extremes, not a universal gradient theorem. A controlled all-collision blend lowers crest84 to50: basin spill changes80->50 and a stale local solve fails. Thus continuous composition must precede drainage commitments or trigger revalidation.

Report `build/junction-forcing.json`; inspected code-generated `build/gallery/junction-forcing.png` compares closest-edge/new/support and two triple-junction sections. CI uploads report/gallery and checks exact fingerprint. Next: broad crustal/continental elevation recipe, fixed world-configuration sea-level policy and multi-seed continental extent/50-55% total-land assessment, THEN separate experimental tectonic viewer with top-bar fields. Do not imply viewer exists yet or attach old unresolved rivers to the new terrain. Full hydrological root/terminal and outlet-preserving refinement remain open in parallel. Keep old production viewer intact.

## Absolute tectonic terrain checkpoint (2026-09-09)

New Java21 `CrustProvinces` + `TectonicTerrain`, `TectonicTerrainGates`; read `research/tectonic-terrain-experiment.md`. Candidate preset S65536/continentalPercent53, Settings radius1500/threshold500/land1800/ocean4200/forcing1000/detail180/sea0/provinceScale4. Scale1 retains old independent crust negative baseline; its geography was sponge-like. Scale4 assigns each EXISTING plate a nearest4S province crust label; exact identity/position/velocity/age unchanged, candidate crust deliberately differs from legacy. Both compact crust interpolation and JunctionForcing use these same labels. Do not overlay legacy crust/regime diagnostics on candidate height.

49-site compact continuous crust fraction -> asymmetric base height + domain-separated four-octave detail at S/8 tapered by signed crust score squared + signed junction forcing. Land iff composed height>fixed seaLevel; below-sea-level is NOT proven ocean. 64 immutable neighborhood LRU, bounded9-site province lookup per plate, no global precomputation. 540 larger-support/independent/cold cases;480 province reference cases; crop/concurrency/counterfactual/coast checks and exact fingerprint10dafe06b67a3c6a1dc165aff64a8d563e3df2af6cf04b5c7ae52fd14bd8141b.

Six fixed wide-area audits (65536 jittered strata over256S) yield51.44-53.94% land, mean52.59%. Local32S regions vary35.88-57.20%. Sampled largest land spans20.125-32S and144-635 plate owners; crop cuts retained, no global component claim. Report `build/tectonic-terrain.json`; inspected gallery `tectonic-terrain.png`, `tectonic-terrain-layers.png`, `tectonic-crust-comparison.png`. Simple plateaus, straight active boundaries, single-scale crust coherence remain candidate limitations. Next separate tectonic viewer/API using this exact model with all controls top-bar and explicit unresolved global hydrology. No current viewer/production/core golden changes in terrain checkpoint.

## Tectonic viewer checkpoint (2026-09-09)

Latest user said continue while terrain checkpoint was finishing; terrain committed/pushed8b1c380 and Windows/Linux CI34387440769 passed. Continued through dedicated viewer at `http://127.0.0.1:8787/tectonics.html`. Read `docs/Tectonic-Viewer.md`. Java21 TectonicView API `/api/tectonic/meta|render|sample`, modeltectonic-experimental/versiontectonic-terrain-v1,14 full configuration controls,17 top-bar layers. TectonicTerrain.boundary uses core closest geometry but candidate crust for regimes/normal/shear. Existing terrain fingerprint unchanged; off-step integer spacing detail wavelength now explicitly floor(S/8).

Page uses standalone tectonics.html/js/css, original and continental pages link to it. Seed BigInt validation/canonicalization, exact ID strings, fixed colors, local-land metric, pan/zoom/keyboard, point inspector, PNG+config export from committed frame. Draft settings marked, apply explicit; revision tokens suppress stale image/point results even if cancellation ignored. No rivers or global ocean claim. Java TectonicViewGates covers17 layers HTTP/direct/crop/zoom, extreme point, validation and GET-only routes; outputs build/tectonic-viewer.json and inspected gallerytectonic-viewer-terrain.png. Optional node tectonic-viewer-contracts.cjs tests real HTTP with minimal DOM, incl delayed responses/export/draft handling; workflow runs it on both platforms against temporary port8790 server.

Computer-use skill inspected/announced for QA, but cua.getBrowser and cua.getState reported no browser/app surfaces; do not claim browser layout/gesture/download visual QA. Use code-generated images and explicit HTTP/application-logic evidence. A hidden Java server was started by this turn at8787 (check actual PID/command before any restart); old server was absent. Logs build/tectonic-server-out.log and build/tectonic-server-error.log. Restart after final compilation so live server uses latest classes. Next actual morphology work: broader mountain belts/interior relief/coast, while root/terminal and outlet-preserving water contracts remain unresolved.

## User requirements

- Minecraft 1.7.10 / GTNH eventually, raw generation first. Follow `docs/Active-Roadmap.md`; preserve the historical `Dev-Roadmap.md` and concept notes. Do not overwrite this repository's Git metadata.
- Infinite, non-repeating plane; deterministic regardless of generation order, crop or zoom. Numeric support ±2^40 is not a planet edge or terminal.
- Continental landmasses, not noise as the sole organizing structure. Placeholder relief is acceptable while larger systems are developed.
- All field controls in the viewer's **top bar**. Dedicated continental viewer requested: actual terrain plus all relevant coast/drainage fields wired to continents.
- Generate diagnostic images from code and inspect them. The old JS prototype is unavailable, not a dependency; do not search for it again. Browser setup is not a blocker.
- Git commits/push authorized. Origin is `https://github.com/xXseesXx/Genesis`, branch main. No force push. No subagents unless explicitly authorized by user/instructions.

## Run and validation

PowerShell cwd: `C:\Users\fabib\Documents\Minecraft\1.7.10\Genesis`.

- `./genesis.ps1 test`: Java 8 core, Java 21 harness/oracle, all Java gates; about 25 seconds locally at this checkpoint.
- `./genesis.ps1 serve`: loopback `http://127.0.0.1:8787/`. Check the listening process before restarting; do not kill unrelated Java processes.
- `./genesis.ps1 gallery`: code-generated candidate images/hashes; does not replace golden baselines.
- Optional `node harness/gates/viewer-contracts.cjs` against the running server: minimal-DOM application-logic tests with real HTTP, not browser layout QA. Node is not required for the Java-only test command.
- CI `.github/workflows/core.yml`: Windows + Ubuntu, JDK21, uploads gallery, core JAR and timings. v4 actions emit deprecation warnings but have passed; upgrading them is separate maintenance.
- `build/` is ignored. Use `apply_patch` for source/text edits. Preserve unrelated user changes.

## Current implementation

`Generator` is the composition root. Two-argument construction is LEGACY; third argument `Generator.Model.CONTINENTAL` selects the integrated continent model. Model + seed + full Params + version define a world. FieldRegistry/TileCache preserve exact typed values; Long identities are JSON strings. 32 scalar fields, one structured column field, 42 numeric parameters. Noise/plate topology remain upstream inputs in both models.

Latest user direction: do independent/easier milestones before returning to water where possible. Implemented M5a geology groundwork, not full M5. `geology/Stratigraphy` consumes registered BASE_ELEVATION/CRUST_TYPE/CRUST_AGE; independent Q4096 seed-domain broad deformation translates three contacts D-3t, D-t, D. Basement basalt/granite, shale (2t), limestone (t), sandstone above; outer units are unbounded material-volume intervals clipped by terrain. Contact ties belong to upper unit. Surface=floor(current uncarved height). Synthetic ages crustAge*(4-index)/4, not historical simulation. Four geometry parameters + ten relative material coefficients. Existing terrain/water unchanged, verified by counterfactuals. Do not feed final eroded exposure back into its own routing/incision inputs. See docs/Stratigraphy.md.

New scalar fields: rockType, rockHardness, rockWeatherability, formationAge, strataDisplacement. `Generator.columns` is a separate typed FieldRegistry with Fields.ROCK_COLUMN (immutable fields/RockColumn DTO); get/values support structured data, numeric tile rejects it. `/api/sample` appends structured column; both inspectors show rock intervals/age/surface marker. All scalar layers remain top-bar controls. Code-generated geology-strata.png inspected (three maps + terrain-clipped section). Eight new column/scalar hashes in geology.properties; continental-world.properties gains new default params only, old hashes unchanged. GeologyGates includes contact/quantization/thickness/age/coefficients, structured cold/bulk/zoom/concurrency, no terrain/water feedback. Budget includes continentalGeology512MedianMs. Full M5 faults/intrusions/event histories/orientation, M6 erosion/scenario loader still absent.

Continental flow: immutable `ContinentalScaffold` → registered raw integer support → `CoastTopology` optional registered score input → fixed coarse triangulation → shared mask/continentality/elevation/bounded drainage → `CoarseChannels`/`BoundaryPorts`. In continental mode the candidate mask aliases committed seaMask, not the un-interpolated raw shape. The raw support remains a clearly labeled diagnostic.

Continental height = coast score * landHeight + coast-tapered tectonic uplift + coast-tapered squared ridged-noise detail (`terrainDetailHeight`, default 900). Those last two terms are exposed separately. Water has bathymetry and zero land-relief terms. This is NOT hydrology-conditioned carving. Legacy field outputs and all 160 historical PNG baselines are unchanged.

The five-lobe scaffold uses connected, seeded macro objects, exact lattice identities, bounded 3x3 support and a bounded immutable-object cache. Objects can merge. Neither the raw union nor its coarse sampled coast establishes globally unique basin identities, global ocean connectivity, or finite upstream dependencies. `docs/Continental-Scaffold.md` contains bounds, exact formulas and limitations.

Routing still searches a bounded Manhattan radius for a sea anchor; rank/direction/distance are -1 when unresolved. Resolved paths strictly decrease rank, but terminals are sea-level anchors, not proven ocean mouths. Guides may cross uphill terrain and are not carved rivers. Never replace unresolved cases with canvas-edge outlets.

`CoarseRunoff` now consumes registered ranks/directions, with exact reverse accumulation of one unit per resolved land anchor; sea contributes zero. Three new fields: `coarseRunoff` (-1 unknown), `coarseRunoffStatus` (-1 unknown / 0 closed under current routing / 1 open frontier), `coarseChannelFlow` (nearest guide's source transfer, zero absent). Unknown adjacent routing and numeric support cuts propagate an open flag downstream. Reverse rank increases bound ancestry to a diamond of radius R-r, max 2,113 anchors, depth 33; per-instance 4,096-entry synchronized LRU. This does not bound the future cascade or solve world roots. `CoarseChannels.channelFlow` carries identical source runoff on both sides of ports, not the destination's total. Renderer uses display-only flow-weighted stroke width, no physical-width/bed claims. All new controls are in both viewers' top bars. See `docs/World-Runoff.md`.

`DrainageRefinement` is a pure four-child kernel: inherited canonical ports, exact parent inflow/local rain/outflow ledger, integer shortest-path resistance, canonical rain remainder splitting and exact accumulation. Tests include exhaustive four-node paths, adjacent parents, composed subdivision, overflow and concurrency. Still lacks world root hierarchy, full corridor H4 enforcement, weighted climate rainfall and physical channel profiles. `refinement.html` is explicitly a synthetic fixture.

The independent Java21 `FiniteHydrology` oracle is finite priority-flood D8 routing, water components and exact flux/window ledgers. `hydrology.html` supports legacy or continental terrain; model survives incoming links, requests and exports. Connected-water mode is conditional on that sampled finite region, not global ocean truth. Oracle code is excluded from the Java8 core JAR.

## Viewer surfaces

- `/`: original field laboratory, legacy model. Continental candidate controls moved off this page; link opens the dedicated page.
- `/continents.html`: separate HTML, shared `app.js`/`style.css`, fixed `data-world=continental`. Top-bar layers, actual height by default, continental overview, pan/zoom, A/B, exact inspector, PNG/config exports. Inapp requests assert returned model. Legacy-only coast controls hidden; tectonic/crust diagnostics explicitly distinguished from land masks.
- `/hydrology.html?model=continental`: finite fill/catchment/runoff analysis of continental heights and mask. Model selector visible.
- `/refinement.html`: independent parent-ledger fixture, not derived from continental terrain.

Relevant code images inspected: `continental-terrain-overview.png`, `continental-terrain-regional.png`, `continental-world-layers.png`. Existing reference/canonical/refinement diagnostics remain available. Do not claim real-browser gesture/layout checks from the minimal-DOM smoke test.

Runoff image inspected: `build/gallery/continental-runoff.png`, seed -1, x=-131072, z=-122880, step256. Four same-coordinate panels show terrain+flow, accumulated units, completeness, and flow guides. It makes excessive parallel coast-directed routes visible; real basin organization and downhill channel conditioning remain next work, not solved by displaying discharge.

## Gates and continuation

New `ContinentalWorldGates`: coast/height/mask agreement, relief independence of coast, shape coverage propagation through drainage and guides, descending routes to continental sea, all-field cold crops/zoom, edge coordinates/nonstandard spacing, evicted concurrent relief queries, eight exact multi-field hashes (`continental-world.properties`). Raw scaffold has its own eight hashes; old PNG baselines unchanged. HTTP tests compare continental render/sample/finite analysis to direct core outputs and reject unknown model values. Timings include continental terrain and guides/ports separately (roughly 100/340 ms locally for 512²).

`RunoffGates`: independent source-forward reference in both models/three seeds; exact arbitrary-window rain+inflow=outflow+terminal+unresolved accounting; maximal dependency diamonds (R32: 10,565 rank reads), water/invalid graph/numeric-edge fixtures; radius-extension stability; source-vs-destination crossing flow; 5,000 shuffled concurrent/evicted odd-spacing points. Existing all-field cold crop/zoom gates now cover 27 fields. HTTP and optional live viewer-logic tests include all three new layers. `build/budget.json` adds continentalRunoff512MedianMs and continentalRivers512MedianMs. Historical world/continent/refinement goldens unchanged.

Remaining before the original M9 scope: infinite-world root/ocean contract and full hydrology cascade; channel/valley terrain conditioning; geological events/stratigraphy/folds/faults; differential erosion and scenario loading; climate; sediment/soil/vegetation; integrated correctness/morphology/performance acceptance. Introduce coarse hardness/rainfall inputs before final dependent commitments to avoid circular generation. Uniform inputs and explicit fixtures are valid early stages.

Latest next-work preference: do independently useful geology faults/intrusions or explicit input fixtures/scenario loading before returning to water where practical. M5a is only a material-volume foundation. When returning to water: multilevel refinement under explicit parent contracts, then downhill channel-bed/valley primitives on current placeholder relief. Continents' visual polish can wait, but global hydrology cannot be called complete without constructive terminal/root commitments. Do not infer that a field interface makes arbitrary noise a valid drainage-root provider. Full M6 erosion, M7 rainfall coupling and M8 sediment still have unfinished water dependencies.

When integrating Minecraft later, import the GTNH ExampleMod starter under `mc-adapter/` without replacing Genesis Git/root files. Caves, ores, full ecology and structures remain parked beyond initial realization.

## 2026-09-10: sparse irregular plate viewer v2

Latest user requested fewer plates, usually one and at most five per continent, mixed land/submerged terrain on almost every plate, serrated irregular shapes, variable size, elevation and tilt. Implemented `IrregularPlates`, `ContinentalGroups`, rewritten `TectonicTerrain` version `tectonic-terrain-v2`. Read [the construction, bounds, evidence and limitations](../research/sparse-irregular-plates.md) and [viewer contracts](Tectonic-Viewer.md) before continuing.

Default spacing524288, warp220, serration140, datum450, tilt600. Core Params spacing maximum expanded to1048576 with legacy larger-support regression; production defaults/golden pixels unchanged. Exact plate/family IDs; family size1–4 with singles dominant. Physical deep envelope protects inter-family boundaries under all allowed relief/sea settings. Weighted/warped shape remains local and deterministic. Some weak plate sites vanish and a family can contain several land components; family count is a budget rather than an observed land-bearing plate count. Fracture age is synthetic, with no event history. The explicit continent limit revives a wide-scale cellular-ocean tendency; next morphology work must acknowledge this instead of claiming larger S resolves it.

Viewer now26 top-bar layers and17 parameters; new plane/identity/size/tilt/fracture fields, three-spacing overview. URL includes version; old v1 links load the new preset with a visible notice, keeping seed and map location. HTTP/direct/common-point tests and Node application logic (including migration, exact IDs and stale requests) pass. Full Java suite passed locally in65s; final targeted terrain/HTTP checks rerun after the final edits. Browser layout/gestures remain unverified.

Terrain fingerprint `e9f11af63c11466bf6ffaea5af7752ff3d4ceb8b22e999337e23563a831c2583`. Six fixed wide-area samples average50.94% land; mixed well-sampled plate rates93–99%. Generated images: `tectonic-terrain.png`, `tectonic-plate-architecture.png`, `tectonic-viewer-terrain.png`. Java server serves8787; recheck listener/process before any restart. PowerShell Start-Process encountered duplicate Path/PATH environment entries, so the server was launched directly as a background tool session.

Untracked `.idea/` and module `.iml` files belong to the user and were left untouched. Stage only task files. Hydrology roots/refinement and Minecraft integration remain deferred; preserve this repo and origin.
## Latest checkpoint: 2026-09-10, motion-driven relief v3

This section supersedes the older checkpoints below. User changed the default land target to30%, requested motion-correlated plate height/tilt, mountainous plate joins and high-contrast height/contours. Read [motion-driven relief](../research/motion-driven-relief.md) and [updated viewer contracts](Tectonic-Viewer.md).

Implemented `PlateResponse` (fixed2S proximity kernel; canonical5×5 anchor support; bounded1,024-entry immutable-result LRU) and `tectonic-terrain-v3`. Broad datum/tilt now use relative motion instead of the old random plane coefficients. Compression widens intra-family crust bridges; inter-family deep margins retain the1–4-plate continent cap. This still permits cellular oceans and is not an exact touching-contact mechanical solver. Fixed−1700 m bed offset gives29.83195% mean land across six seeds (range29.248–30.304%). Mixed well-sampled plates84.8–88.8%; additional wholly submerged plates are expected after lowering. One-block crossing scan finds60 collision mountain joins, example x4430577,z3408189,height1268.874.

Viewer now27 fields,18 world parameters and separate top-bar `contourInterval` (0 off,5–1000 model metres). `heightMap` uses high-contrast fixed colors and halo-based contour strokes, heavier every fifth line. Requested spacing doubles at wide zooms; actual spacing is shown and returned in an HTTP header. Crops at the same scale agree; world heights/unadorned colors are zoom-invariant, contour strokes intentionally are not. URL/PNG+config export includes display state. Saved v1/v2 links visibly migrate to the new preset. Code-generated height/mountain PNGs inspected; browser layout/gestures not claimed.

Full local Java suite passed in73.72s; Node viewer logic passed. New counterfactual, response-support/cache, mountain-crossing, contour-halo/5m/invalid-input checks pass. V3 fingerprint `320631683b8b458d4603d86873efe8d65a07c5a643b17d247ce6c77f42c31413`. Production core and old goldens unchanged. CI runs on pushed commit; inspect latest GitHub run after resuming rather than assuming its outcome from this note.

Live server restarted at8787 using direct unified exec (session69196); v3 metadata verified by the Node tests. For a future restart, discover the exact current Java command/PID first; do not reuse old PID21100. `Start-Process` had Path/PATH environment-key problems in this local environment, so direct background exec was used. User IDE files remain untracked and untouched: `.idea/`, `core/core.iml`, `oracle/oracle.iml`, `harness/gates/gates.iml`, `harness/server/server.iml`.

Next: actual curved contact normals/contact-aware broad response, smooth mixed-crust forcing transitions, less cellular bounded-family shapes and richer mountain interiors. Hydrological terminal completeness and outlet-preserving refinement are still open; plate families are not drainage roots. Keep Minecraft deferred and preserve this repo.
